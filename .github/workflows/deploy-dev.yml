name: Deploy Dev Environment

on:
  push:
    branches: [dev]
  workflow_dispatch:
    inputs:
      branch:
        description: Branch to deploy (dev only)
        required: true
        default: dev
        type: choice
        options:
          - dev

concurrency:
  group: deploy-dev-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  deploy_supabase_dev:
    name: Deploy Supabase Migrations (Dev)
    if: >-
      (github.event_name == 'workflow_dispatch' && github.event.inputs.branch == 'dev') ||
      (github.event_name == 'push' && github.ref_name == 'dev')
    runs-on: ubuntu-latest
    env:
      SUPABASE_ACCESS_TOKEN: ${{ secrets.DEV_SUPABASE_ACCESS_TOKEN || secrets.SUPABASE_ACCESS_TOKEN }}
      SUPABASE_DB_PASSWORD: ${{ secrets.DEV_SUPABASE_DB_PASSWORD || secrets.SUPABASE_DB_PASSWORD }}
      SUPABASE_PROJECT_REF: ${{ secrets.DEV_SUPABASE_PROJECT_REF || secrets.SUPABASE_PROJECT_REF }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate required Supabase secrets
        shell: bash
        run: |
          set -euo pipefail
          [[ -n "${SUPABASE_ACCESS_TOKEN:-}" ]] || { echo "Missing secret: DEV_SUPABASE_ACCESS_TOKEN (or SUPABASE_ACCESS_TOKEN)"; exit 1; }
          [[ -n "${SUPABASE_DB_PASSWORD:-}" ]] || { echo "Missing secret: DEV_SUPABASE_DB_PASSWORD (or SUPABASE_DB_PASSWORD)"; exit 1; }
          [[ -n "${SUPABASE_PROJECT_REF:-}" ]] || { echo "Missing secret: DEV_SUPABASE_PROJECT_REF (or SUPABASE_PROJECT_REF)"; exit 1; }

      - name: Setup Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Link Supabase project
        run: supabase link --project-ref "$SUPABASE_PROJECT_REF"

      - name: Push database migrations
        run: supabase db push --linked --password "$SUPABASE_DB_PASSWORD"

  deploy_vercel_dev:
    name: Deploy Vercel (Dev)
    if: >-
      (github.event_name == 'workflow_dispatch' && github.event.inputs.branch == 'dev') ||
      (github.event_name == 'push' && github.ref_name == 'dev')
    runs-on: ubuntu-latest
    needs: [deploy_supabase_dev]
    env:
      VERCEL_TOKEN: ${{ secrets.DEV_VERCEL_TOKEN || secrets.VERCEL_TOKEN }}
      VERCEL_ORG_ID: ${{ secrets.DEV_VERCEL_ORG_ID || secrets.VERCEL_ORG_ID }}
      VERCEL_PROJECT_ID: ${{ secrets.DEV_VERCEL_PROJECT_ID || secrets.VERCEL_PROJECT_ID }}
      HEALTHCHECK_URL: ${{ secrets.DEV_HEALTHCHECK_URL }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate required Vercel secrets
        shell: bash
        run: |
          set -euo pipefail
          [[ -n "${VERCEL_TOKEN:-}" ]] || { echo "Missing secret: DEV_VERCEL_TOKEN (or VERCEL_TOKEN)"; exit 1; }
          [[ -n "${VERCEL_ORG_ID:-}" ]] || { echo "Missing secret: DEV_VERCEL_ORG_ID (or VERCEL_ORG_ID)"; exit 1; }
          [[ -n "${VERCEL_PROJECT_ID:-}" ]] || { echo "Missing secret: DEV_VERCEL_PROJECT_ID (or VERCEL_PROJECT_ID)"; exit 1; }

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Vercel CLI
        run: npm install --global vercel@latest

      - name: Pull Vercel environment configuration (preview)
        run: vercel pull --yes --environment=preview --token "$VERCEL_TOKEN"

      - name: Build deployment artifacts
        run: vercel build --token "$VERCEL_TOKEN"

      - name: Deploy to Vercel preview
        run: vercel deploy --prebuilt --token "$VERCEL_TOKEN"

      - name: Post-deploy health check
        shell: bash
        run: |
          set -euo pipefail
          if [[ -z "${HEALTHCHECK_URL:-}" ]]; then
            echo "DEV_HEALTHCHECK_URL secret is not set."
            exit 1
          fi
          vercel curl "$HEALTHCHECK_URL" \
            --token "$VERCEL_TOKEN" \
            --fail \
            --silent \
            --show-error \
            --retry 8 \
            --retry-delay 5 \
            --retry-all-errors
